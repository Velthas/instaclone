import React from "react";
import styled from "styled-components";

const Notifications = () => {
  //TODO: IMPLEMENT NOTIFICATIONS FOR USER ACTIONS
  return (
    <div>
      <h1>Notifications</h1>
      <div>
        Empty for now, will come back for this once all the main features are up and running
      </div>
    </div>
  )
};

const Container = styled.nav`
  display: flex;
`;

export default Notifications

